#include<stdio.h>

/*
Structures:
Keyword struct is used for creating a structure.
The format for declaring a structure is as below:
    struct <structure name>{
        data_type member1;
        datat_type member2;
        .
        .
        .
        data_type membern;
    };

Note: No memory location for declaration\description of structure.



*/

typedef long long l1;
int main(){
    l1 a = 20;
    printf("%ld", a);
    return 0;
}