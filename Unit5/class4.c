#include<stdio.h>

/* Enums :
        Used to store constant values in the code.
        Scanf cannot be used on it as it is a constant and no actual memory is allocated.*/

enum months 
{
    jan=1,march,apr,may,jun,july,aug,sept,oct,nov,dec
};

enum date{
    max = 10, min
};
int main(){
        printf("%d\n",jan);
}